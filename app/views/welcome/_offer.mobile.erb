<li>
  <a href="<%= current_user ? viewdeal_path(offer) : new_user_registration_path %>">
    <img src="/images/<%= offer.category.parent_or_friendly_id %>.png" class="ui-li-icon"/>
    <h3><%= offer.title %></h3>
    <% if current_user %>
      <p><strong><%= offer.user.name %></strong></p>
      <p><%= offer.description.try(:html_safe) %></p>
    <% end %>
  </a>
</li>
