<address>
  <span><%= offer_address.businesses.first.name %></span>
  <span><%= offer_address.businesses.first.street_address_1 %></span>
  <% if offer_address.businesses.first.street_address_2 %>
  <span><%= offer_address.businesses.first.street_address_2 %></span>
  <% end %>
  <span><%= offer_address.businesses.first.city %>, <%= offer_address.businesses.first.state %> <%= offer_address.businesses.first.postal_code%></span>
</address>

