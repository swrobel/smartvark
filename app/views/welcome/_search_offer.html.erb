<div class="item">
  <div class="text">
    <h3><span>
          <%= link_to "#{search_offer.title}" , viewdeal_path(search_offer) %>
        </span>
      </h3>
    <p><%= search_offer.description %></p>
  </div>
  <%= render :partial => 'offer_address', :object=> search_offer %>
</div>

